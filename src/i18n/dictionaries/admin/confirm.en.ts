export const confirm = {
      defaultTitle: 'Are you sure?',
      // Button labels summarise the outcome — "Yes/No" is banned by the standard (§4.7).
      // Callers should pass a better verb+noun ("Delete product"); these are only the
      // safe fallbacks used when nothing is supplied.
      confirm: 'Continue',
      cancel: 'Cancel',
      delete: 'Delete',
      discardChanges: 'Discard changes',
      typeToConfirm: 'Type "{value}" to confirm'
};
