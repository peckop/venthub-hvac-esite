---
domain: general
source_type: doc
namespace_type: module
source_path: C:\tmp\wt-supurme\src\views\admin\__tests__\AdminErrorGroupsPage.integration.test.tsx
skeleton_hash: ac592f86afb7c1c4
entity_hashes:
  overview: 1cd10a7aacc90ca5
  style_tokens: dd5ed8d0f58dcf57
generated_at: 2026-08-25T07:32:46Z
---

## Genel Bakış
Bu modül, `AdminErrorGroupsPage` bileşeninin entegrasyon testlerini içerir. Testler, `@testing-library/react` ve `vitest` kütüphaneleri kullanılarak yazılmıştır ve bileşenin erişilebilirlik standartlarına uygunluğunu `testA11y` fonksiyonu ile kontrol eder. Modülde `sb` adında bir sabit tanımlanmıştır, ancak bu sabitin ne işe yaradığı bilinmiyor. Doğrudan bir API veya tablo sorgulaması yapılmaz; bunun yerine, bileşenin davranışını test etmek için gerekli mock'lar kullanılır.

---

## AXIOMS – Mimari Varsayımlar

Bu modül için özel aksiyom tanımlanmamıştır.

**Gerekçe:** Modül bir test dosyasıdır (`AdminErrorGroupsPage.integration.test.tsx`). Fonksiyon imzaları verilmemiştir; dolayısıyla fonksiyon gövdesinden çıkarım yapılacak bir kaynak bulunmamaktadır. Modül sabiti olarak yalnızca `sb (call)` belirtilmiş olup, bu sabitin ne amaçla kullanıldığına dair fonksiyon gövdesi bilgisi mevcut değildir.

---

## FONKSİYON DETAYLARI

---

## İTHALATLAR (IMPORTS)
- import: ../AdminErrorGroupsPage::AdminErrorGroupsPage
- import: @/utils/testA11y::testA11y
- import: @testing-library/react::render
- import: @testing-library/react::screen
- import: react::React
- import: vitest::describe
- import: vitest::expect
- import: vitest::it
- import: vitest::vi

---

## SABİTLER
- **sb** (call) — `vi.hoisted(() => {
  const groupsData = [
    {
      id: 'g1111111-aaaa',...`

---

## NODE ID STANDARD

  file: AdminErrorGroupsPage.integration.test.tsx

---

## STİL TOKENLERİ

### Arbitrary Değerler (token'a geçirilmemiş)
Yok — tüm stiller token'a geçirilmiş. ✅

### Kullanılan Token'lar (zaten token'a geçirilmiş)
- (yok)

### Tailwind Sınıf Özeti
- **Renkler:** (yok)
- **Layout:** (yok)
- **Varyant/Responsive:** (yok)
- **Yardımcı Sınıflar:** (yok)