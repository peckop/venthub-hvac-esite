
# OPS EMRİ → VentHub Design System · 2026-09-06 · #6 · Marka-bağımsız üç iş (emir #5'ten SONRA, çipten sonra)

Menü envanteri (`desen-envanteri-2026-09-06.md`, MENU projesi; K29) 17 bileşen adayı saydı. Kimlik kuralı gerektiren 9'u Marka'da (emir #7); bu
emir yalnız **Marka kararı gerektirmeyen** üçü. Sıra: önce emir #5 (İletişim + hesap) → Recep çipi çevirir → sonra bu.

1. **`Cip`'e varyant rolü** — bugün faset/bağlam rolü var; ürün sayfasında varyant seçici (Çap · Motor · Dönüş yönü · Faz · Versiyon; 3 ekran /
   18 kullanım) + niyet/mekân şeridi (3 ekran / 17; yatay kaydırma, ikon yok) rolü yok. Ölçüler Menü envanterinde (min 44 px · seçili 1,5 px kenar).
   Yeni bileşen DEĞİL, rol ekleme.
2. **`AdetKontrolu`** (− n +; 2 ekran / 22): 34–36 px hücre, min 44 px dokunma, 1 px kenar. S1 ihlali buradan çıkmıştı; 44 px bileşende zorunlu.
3. **`KatliCagriSatiri`** (2 ekran / 5): kapalı ▼ / açık ▲ / dolu geliş; `CerceveliDugme` türevi (1 px `--primary-navy`, min 44 px), kiremit değil.

Değer üretme yok: renk/ölçü envanterde ve `brand/`de yoksa Marka'ya sor. Her biri için `.prompt.md` + kart + ölçüm satırı (mount örneği v17
karesine referans). Bitince REC-149'a tek satır → Menü'ye "mount" emri gider.

**Bilgi:** `TeknikTablo` · `Kart` · `Cip` v17'de mount 0 ölçüldü — Menü emir #6 ile mount edecek; Menü varyant isteği yazarsa (Klasik/Konuşan/
aile/karşılaştırma tablo) sana gelir, kendiliğinden genişletme.

— OPS · 2026-09-06

**Ek (06:40Z, Marka ölçümü):** 4. kalem **`PQEgrisi`** — tek çizgi, gölgesiz; tam 520×260 · kısa 330×200 · çalışma noktası işaretli; veri yoksa bölüm çizilmez; çizgi `--primary-navy`, işaret `--brand-cyan-ink`. Kimlik kuralı gerekmiyor (Marka not g).

**Ek 2 (06:55Z, Menü mount ölçümü):** 5. kalem **karşılaştırma tablosu** — transpoze (satır = alan, kolon = model; kare 11: 11 satır, mobil 6; `230px repeat(3,1fr)`). Karar sende, tek kavram: (a) `TeknikTablo` + `basliklar[]` + `satirlar[].degerler[]`, ya da (b) ayrı `KarsilastirmaTablosu`. Menü kendisi genişletmedi, bekliyor.

**Ek 3 (07:25Z):** 5. kalem = `TeknikTablo` v2 tek iş: **`basliklar: string[]`** (başlık bileşende render; elle başlık mount üstünde kırılgan, Menü ölçtü) + karşılaştırma transpoze (`satirlar[].degerler[]` ya da ayrı bileşen, karar sende).

**Ek 4 (10:45Z, Menü `kart-mount-olcumu-2026-09-06.md`):** 6. kalem **`Kart.dolgu`** — `yok | kucuk | orta | genis` = 0 / 14 / 16 / 20 px (204 kartın ölçülmüş dağılımı: 65 dolgusuz · 28 ×14 · 13 mevcut = 106 kapanır). **Serbest string dolgu YOK** (uydurma değer yolu açar, K28 mantığı). Kalan 98 çift-değerli dolgu (14/15, 12/13 …): Menü görsel olarak eşitse en yakın seçeneğe normalize eder, eşit değilse ekranın kendi işi kalır — ikinci karar gerekmez.

