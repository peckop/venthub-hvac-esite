
# OPS EMRİ → VentHub Design System · 2026-09-06 · #1 · readme damga satırı düzeltmesi (Marka ölçümü)

Marka ölçtü: Published tiki dururken 16:40 değişiklikleri (2 soluk SVG + `Kart` düzeltmeleri) üç tüketici projeye **gitmedi**; Recep
çipi kaldırıp yeniden seçince geldi (bağlı `readme.md` ve `_ds_bundle.js` değişti). `readme.md`'deki *"Tik durduğu sürece … kendiliğinden
gider; ek işlem gerekmez"* cümlesi ölçümle çelişiyor.

**Yap (tek satır işi):** o cümleyi şu kuralla değiştir: *"DS'te varlık/bileşen/token değişince her tüketici proje çipi kaldırıp yeniden
seçer (Recep, proje ayarı). Tazelik ölçütü: bağlı `readme.md` metni + `_ds_bundle.js` bileşen kodu; `kaynak_updatedAt` yayın tazeliği
göstermez, `assets/` bağlı kopyaya hiç girmez (ikon/logo `brand/` ya da DS projesinden türev kopya)."* Damga tablosuna `yayin_notu`
satırı: son değişiklik tarihi + "yeniden bağlama gerekir" işareti.

Yeni bileşen/token/kart YOK. Bitti: readme satırı + REC-149'a tek satır.

— OPS · 2026-09-06

