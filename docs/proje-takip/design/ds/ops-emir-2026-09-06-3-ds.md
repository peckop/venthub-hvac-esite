
# OPS EMRİ → VentHub Design System / DESIGN-MARKA · 2026-09-06 · #3 · Emir #2'nin düzeltmesi: iki token (K25-b)

Emir #2 madde 2 ("KabukBandi sayaç metni lacivert") İPTAL — OPS ölçmeden yazdı, Menü ölçtü: lacivert/turkuaz 3,47 (beyazdan kötü).
Doğrusu zemini koyulaştırmak. Kararlar 15A **K25-b**:

1. **`--brand-cyan-ink`** (emir #2 madde 1 ile aynı token) iki işte kullanılır: küçük turkuaz metin/bağlantı rengi **ve** sayaç/rozet
   zemini (metin beyaz). Şart: beyaz ve `#F4F4F2` üstünde metin olarak ≥4,5:1 **ve** beyaz metin altında zemin olarak ≥4,5:1.
   Hedef ≈`#00708F` (beyaz/#00708F ≈5,2). Sayı senin, kılavuza yaz.
2. **`--action-terracotta-deep`**: kiremit düğme zemini; beyaz metinle ≥4,5:1 (bugünkü kiremit 3,87). `AnaEylemDugmesi` zemini bu tokene
   geçer; eski `--action-terracotta` zemin/vurgu olarak kalır (metin değil). Görünür sonuç: kiremit düğme bir ton koyulaşır — Recep'e
   bilgi gitti.
3. `KabukBandi` sayaç: zemin `--brand-cyan-ink`, metin beyaz (emir #2'deki "lacivert" satırını uygulama).
4. Emir #1 (readme `yayin_notu`) + emir #2 madde 3–4 (`--text-muted` kuralı, readme) aynı yayında.
5. **(ek, 22:25Z — Belge föy ölçümü)** `TeknikTablo` değer kolonu sabit 150 px ve `style` prop'u yalnız köke iniyor; föyde 8/16 hücre taştı,
   5'i anlam sütununun üstüne bastı (Belge kendi tarafında dizeyi kısaltarak çözdü). Bileşene **`kolonlar`** prop'u: `{ alan, deger, anlam }`
   px değerleri, varsayılan bugünkü `190 / 150 / 1fr`; taşma davranışı `overflow-wrap:anywhere` varsayılan. Kart örneğine bir uzun-değer satırı.

Bitti: iki token + bileşen iki değişiklik + kılavuz satırları + REC-149'a ölçülen oranlar. Sonra Recep üç projede çipi yeniden seçer.

— OPS · 2026-09-06

