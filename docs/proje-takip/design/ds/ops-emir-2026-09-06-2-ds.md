
# OPS EMRİ → VentHub Design System / DESIGN-MARKA · 2026-09-06 · #2 · `--brand-cyan-ink` tokeni + sayaç metin rengi (Kararlar 15A K25)

Menü ölçtü (20:45Z): `--brand-cyan` (#0088B0) küçük metin olarak beyazda 4,02 · `#F4F4F2` üstünde 3,70 → AA (4,5) altında. Kullanım 72 (mono
bölüm etiketleri) + 59 (Teklif sayacı beyaz metin). Kararlar 15A **K25**: turkuaz METİN rengi değildir, zemin/kenar rengidir; metin için
ayrı mürekkep tonu.

**Yap:**
1. Yeni token **`--brand-cyan-ink`**: turkuaz ailesinden, **beyaz ve `#F4F4F2` üstünde ≥ 4,5:1** (hedef ≈ `#00708F`; sen ölç, sayı senin).
   `tokens/renk.css` + `styles.css` + `brand/tokens.css` (tek DEĞER dosyası kuralı) + renk kılavuzu satırı (kullanım: küçük etiket, bağlantı metni).
2. `KabukBandi` sayaç: metin rengi beyaz → **`--primary-navy`** (turkuaz zemin üstüne lacivert; oranı yaz). Prop gerekmiyor, bileşen içi sabit.
3. `--text-muted` uyarısı (Menü): beyazda 4,83 ama `#F4F4F2` sayfa zemininde 4,39. Hüküm: token DEĞİŞMEZ; kılavuza kural satırı: "`--text-muted`
   yalnız kart/beyaz yüzeyde; sayfa zemininde küçük metin `--text-body`". Yeni üçüncü gri ÜRETİLMEZ (hiyerarşi boy+ağırlıkla).
4. readme `yayin_notu` satırı + "yeniden bağlama gerekir" işareti (emir 09-06 #1 ile birlikte tek yayın).

Bitti: token + bileşen + kılavuz satırı + REC-149'a tek yorum (ölçülen oranlar). Recep sonra MENU/BELGE/MARKA'da çipi kaldır-yeniden seçer.

— OPS · 2026-09-06

