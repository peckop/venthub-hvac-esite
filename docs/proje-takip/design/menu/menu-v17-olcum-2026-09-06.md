
# "Menü Tasarımı v17.dc.html" — Bağımsız Ölçüm (2026-09-06)

Kaynak: Claude Design projesi `be615496-25af-4630-aa2c-324c2cfc88e0`, dosya `Menü Tasarımı v17.dc.html`.
İndirme: `design_dl.py` (tek istek) 256 KiB kapısında kesiliyordu (259.617 bayt) — gerçek dosya 908.588 bayt (900.222 karakter) olduğu için `design_dl_full.py` (satır penceresi) ile eksiksiz indirildi ve UTF-8 doğrulandı (`file` → "Unicode text, UTF-8"). Terminalde görülen "�" karakterleri yalnız Windows konsol kod sayfası sorunuydu; dosyanın kendisi bozuk değil.

Ölçüm scripti: ad-hoc Python + `re`, çıktı ara dosyalara (`measure*.txt`) yazılıp okundu (Windows konsolu UTF-8 Türkçe karakterleri bozduğu için doğrudan print yerine dosyaya yazma tercih edildi).

| # | İddia | Menü'nün Beyanı | Ölçümüm | Ölçüt (regex/yöntem) | Hüküm |
|---|---|---|---|---|---|
| 1 | "30 kare" | 30 kare | **Yapısal olarak 30** `data-screen-label` içeren `<section>` var (toplam `<section>`=31, 1 tanesi etiketsiz kapak bölümü). AMA bu 30'un **2'si tekil kare değil GRUP etiketi**: `M1-M9` (mobil kabuk, 9 alt-ekran) ve `S1-S6` (satış kipi, 6 alt-ekran) — yani tek-kare (1440px masaüstü) sayısı aslında **28**. Belgenin **kendi başlık satırı** da "30" değil "**29 kare**" diyor: `VentHub · tek dosya · 29 kare + mobil kabuk M1–M9 + satış kipi S1–S6 · v17 · 1440 + 390`. | `data-screen-label="..."` attr sayımı; `<section` / `</section>` sayımı; başlık metni regex `VentHub[^<]*kare` | **TUTMUYOR (tam sayı uyuşmuyor)** — üç farklı doğru sayı var (31 section / 30 etiket / 28 tekil-kare / 29 belgenin kendi beyanı) ve hiçbiri net "30 kare" değil. En yakın doğru okuma: 30 = section-etiketi sayısı ama bunun 2'si grup, belge kendi içinde 29 diyor. |
| 2 | "filter 0" | 0 kullanım | **0** | `filter\s*:` → 0, `filter\s*=` → 0, `grayscale` → 0, `backdrop-filter\s*:` → 0 | **TUTUYOR** |
| 3 | "opacity 0" | 0 kullanım | **2** (ikisi de `opacity:1`, img şeritleri dışında; img etiketleri içinde opacity kullanımı 0/192) | `opacity\s*:` toplam=2; `<img[^>]*>` içinde opacity geçen: 0/192 | **TUTMUYOR** — "opacity: hiç yok" iddiası yanlış; 2 kullanım var (ikisi `opacity:1`, görsel etkisi yok ama string olarak mevcut) |
| 4 | "83/88 bant KabukBandi" | 83/88 | **Payda (88) TUTUYOR, pay (83) ÖLÇÜLEMEDİ.** `KabukBandi` x-import toplamı = **88** (`rol="header"`=84, `rol="footer"`=4). Belge metninde "83/88" ifadesi hiç geçmiyor; 88'in hangi alt-kümesinin "83" olduğunu ayırt edecek bir işaret (ör. imzası tutarlı olanlar) bulunamadı — 11 farklı imza/varyant var, hiçbiri 83'e denk gelmiyor. Ayrıca `x-import` toplamı=149 (KabukBandi 88 + CerceveliDugme + AnaEylemDugmesi diğerleri), ham `<header` etiketi=0 (hepsi `x-import` komponenti, düz HTML `<header>` yok). | `<x-import[^>]*KabukBandi[^>]*>` sayımı, `rol="header"`/`rol="footer"` alt-sayım, imza (Counter) kümeleme; `<header[ >]` | **KISMEN TUTUYOR** (88 doğru) / **83 ÖLÇÜLEMEDİ** |
| 5 | "--brand-cyan metin olarak 0" | 0 | **0** — `color:var(--brand-cyan)` (bare, `-ink` hariç) = 0, `color:hsl(var(--brand-cyan))` (bare) = 0. Not: `--brand-cyan-ink` (farklı, metin-güvenli token) 251 kez kullanılıyor — bu ayrı ve beklenen bir kullanım, ihlal değil. `--action-terracotta-deep` = 36. Ham `#d95d0e`/`#D95D0E` = 4 (hepsi `stroke`/`outline`, `color:` değil). Ham `#0088B0` (herhangi case) = **39** — ama incelendiğinde tamamı `box-shadow`, `border-left`, SVG `stroke`/`fill` içinde; `color:` özelliği olarak 0. | `color\s*:\s*var\(--brand-cyan\)`, `color\s*:\s*hsl\(var\(--brand-cyan\)\)`, `--brand-cyan-ink` sayımı, `#[dD]95[dD]0[eE]`, `#0088[bB]0`, `color\s*:\s*#0088[bB]0` | **TUTUYOR** (dar tanımla: `--brand-cyan` marka rengi hiçbir yerde `color:` metin rengi olarak kullanılmıyor). Not: 39 kez ham `#0088b0` hex'i dekoratif (altı çizili sekme, sol kenarlık, SVG çizgi) olarak var — token değil ham hex, ayrı bir design-token ihlali olabilir ama "metin rengi" iddiasını çürütmez. |
| 6 | "ARŞİV · ÇİZİLMEZ (K24)" etiketi | var | **Tam eşleşme YOK.** Belgede `ARŞİV · çizilmez (K24)` var (küçük harfle "çizilmez", "ÇİZİLMEZ" değil) — 1 kez. `ARŞİV` tek başına 3 kez geçiyor (biri düz etiket, biri bu ifade, biri düz metin cümlesi içinde). | `ARŞİV\s*·\s*ÇİZİLMEZ\s*\(K24\)` → 0; `ARŞİV\s*·\s*çizilmez\s*\(K24\)` → 1; `ARŞİV` (any) → 3 | **TUTMUYOR (harf büyüklüğü farklı)** — anlam olarak var ama iddia edilen tam biçim (büyük harfli ÇİZİLMEZ) yok |
| 7 | "SEÇİLDİ · K24" etiketi | var | **1** tam eşleşme bulundu. `SEÇİLDİ` tek başına 2 kez (biri bu etiket, biri "52b · SEÇİLDİ — sağ üstte yalnız hesap..." cümlesi). | `SEÇİLDİ\s*·\s*K24` → 1 | **TUTUYOR** |
| 8 | "S1–S6" satış kipi kareleri + "KAPALI BEKLER" etiketi | var | S1–S6 etiketleri `data-screen-label="S1-S6"` grup section'ı içinde teker teker span olarak var (6/6 doğrulandı, SVG path `d="M..."` gibi yanlış eşleşmeler ayıklanarak). `KAPALI BEKLER` metni **19** kez geçiyor. `NEXT_PUBLIC_ODEME_ACIK` **7** kez geçiyor. | Manuel filtreli regex (`(?<![\"\d.])S%d(?![0-9.])`), `KAPALI\s+BEKLER`, `NEXT_PUBLIC_ODEME_ACIK` | **TUTUYOR** |
| 9 | "M1–M9" mobil kare etiketleri | var | 9/9 doğrulandı — her biri (`>M1<`...`>M9<`) tam olarak 1'er kez etiket olarak geçiyor, artı başlıkta 2 özet referans. (Ham `\bM3\b` gibi naif regex'ler SVG path verisi `d="M3 4.6h14..."` yüzünden 135 gibi sahte sayılar veriyor — bu tuzağa düşülmemesi gerekiyor.) | Bağlam-filtreli regex, `data-screen-label="M1-M9"` section doğrulaması | **TUTUYOR** |
| 10 | Türkçe "Teklif al" (K5 yasak) | 0 gerçek kullanım | Ham string olarak **1** kez geçiyor ama bu tek geçiş bir **tasarımcı notu/altyazısı** içinde: *"Fiil tek: 'Teklif iste'; 'Teklif al' hiçbir yerde yok."* — yani UI'da gerçek bir buton/etiket olarak değil, yokluğunu teyit eden meta-yorum olarak geçiyor. Gerçek UI metni olarak **0**. "Teklif iste" ise **9** kez (buton/etiket olarak gerçek kullanım). | `Teklif al\b`, `Teklif iste\b`, bağlam okuma | **TUTUYOR** (UI'da gerçek kullanım 0; ham grep 1 döndürür ama o da yokluğu teyit eden metin) |
| 11 | "AVENS" (büyük harf, yasak) | 0 | **0** | `\bAVENS\b` (case-sensitive) → 0 | **TUTUYOR** |
| 12 | "AVenS" (doğru yazım) | var | **6** kez — hepsi `img alt="AVenS"` veya düz metin etiketi olarak, tutarlı. | `\bAVenS\b` (case-sensitive) → 6 | **TUTUYOR** |
| 13 | Ham hex renk toplamı + en sık 10 | ölçülecek | **999** ham 6-haneli hex (`#rrggbb`) geçişi, **37** farklı değer. En sık 10: `#d8d8d4`(426), `#f2f2ee`(142), `#fbfbf9`(74), `#24395c`(45), `#0088b0`(39), `#f6f6f3`(23), `#ffffff`(22), `#1a2b4a`(21), `#256540`(19), `#e5f3f8`(18). (3 haneli hex: 4 kez.) | `#[0-9a-fA-F]{6}\b` + `Counter` | **ÖLÇÜLDÜ** (iddia yok, ham sayı — CLAUDE.md kural-8 "arbitrary/ham hex yasak, HSL token kullan" ile karşılaştırıldığında 999 kullanım dikkat çekici; büyük kısmı muhtemelen SVG ikon/diyagram `fill`/`stroke` içinde, kesin oran ölçülmedi) |

## Ek gözlem (istenmedi ama ölçüm sırasında bulundu)
- Dosya boyutu iddiası ("~900 KB") **TUTUYOR**: gerçek 908.588 bayt.
- Tek-istekli `design_dl.py` bu boyuttaki dosyada sessizce 256 KiB'de kesiyor (259.617 bayt döndü, hata vermedi) — ileride bu dosyayla çalışacak her ölçüm için `design_dl_full.py` zorunlu, yoksa "kesik dosya = eksik/yanlış negatif" riski var.
- `width:1440px` ham CSS string'i 34 kez, `width:390px` 48 kez geçiyor — ama bunlar kare-başı bir kez değil, kare içi iç-içe konteynerlerde tekrarlanıyor; bu yüzden kare sayımı için güvenilir değil (madde 1'de bu yüzden `data-screen-label` kullanıldı).

## Özet (5 satır)
- 13 iddiadan **7 TUTUYOR** (filter=0, --brand-cyan metin=0, SEÇİLDİ·K24, S1-S6/KAPALI BEKLER/NEXT_PUBLIC_ODEME_ACIK, M1-M9, AVENS=0, AVenS=6, Teklif al=0-gerçek-kullanım) — bazıları nüanslı TUTUYOR.
- **3 TUTMUYOR**: "30 kare" (yapısal 28 tekil + 2 grup = 30 etiket ama belgenin kendi başlığı 29 diyor, hiçbiri net "30" değil), "opacity 0" (gerçekte 2 kullanım var), "ARŞİV · ÇİZİLMEZ (K24)" (büyük harfle yok, küçük harfle "çizilmez" var).
- **1 KISMEN/ÖLÇÜLEMEDİ**: "83/88 KabukBandi" — 88 payda doğrulandı, 83 payı belgede hiçbir yerde geçmiyor ve hangi alt-kümeye denk geldiği tespit edilemedi.
- En önemli bulgu: "30 kare" rakamı gevşek — güvenilir sayı üçe bölünüyor (31 section / 30 etiket-ki-2'si grup / 28 tekil-kare / 29 belgenin kendi beyanı); hangi tanım kullanılırsa kullanılsın "30" hiçbirine tam denk gelmiyor.
- Yöntemsel not: tek-istekli indirme sessizce 256 KiB'de kesiliyor ve naif `\bM3\b` gibi regex'ler SVG path verisiyle (`d="M3..."`) çakışıp sahte yüzlerce eşleşme veriyor — ikisi de bu tür ölçümlerde bilinmesi gereken tuzaklar.

