// This module has been intentionally removed.
// Reason: contained hardcoded sandbox credentials and is not used by the active edge function (index.ts).
// If you need a real İyzico integration helper, create a new module that reads all secrets from environment variables
// and NEVER commit secrets to the repository.

export {};
